apiVersion: v1
kind: Service
metadata:
  name: watcher
  labels:
    {{- include "watcher.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: watcher
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
