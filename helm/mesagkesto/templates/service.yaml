apiVersion: v1
kind: Service
metadata:
  name: mesagkesto
  labels:
    {{- include "mesagkesto.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: mesagkesto
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
      protocol: TCP
